#include "minishell.h"

/*
read_in     -> operator와 string을 보고 실행되는 건지 확인.
                가장 마지막 것만 인풋으로 넣음
read_out    ->
read_out2   ->
*/
void	redirection(t_global *global)
{
    (void)global;
    printf("redi start\n");
}
